{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm run server:build"
  },
  "deploy": {
    "startCommand": "npx drizzle-kit push && npm run server:prod",
    "restartPolicyType": "ON_FAILURE"
  }
}
